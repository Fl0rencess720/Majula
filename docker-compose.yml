services:
  majula:
    build: .
    container_name: majula
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./configs/.env
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./zap.log:/app/zap.log
      - ./configs/config.yaml:/app/configs/config.yaml
    networks:
      - majula_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  majula_network:
    driver: bridge